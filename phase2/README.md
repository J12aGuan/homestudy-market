# Phase 2 — standalone survey (not started)

Phase II is now its own Qualtrics survey. Nothing has been built here yet.

The working joint-survey implementation of the bidding game lives in
`../combined/` (`phase2.qualtrics.js`, `phase2_instructions.html`,
`phase2_quiz_intro.html`, `core_strategy.html`) and is the starting point when we
pick Phase II back up.

Open design questions for Phase II are tracked in `~/Downloads/pi_decisions.md`;
the largest one is how each house's reference price is set once Phase II no longer
follows Phase I in the same session.
